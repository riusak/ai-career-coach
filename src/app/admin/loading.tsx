import { AdminSkeleton } from '@/components/ui/dashboard-skeletons';

/** Chart 4 — instant loading state for the admin overview segment. */
export default function Loading() {
  return <AdminSkeleton />;
}
