import { AnalyticsSkeleton } from '@/components/ui/dashboard-skeletons';

/** Chart 4 — instant loading state for the Analytics segment. */
export default function Loading() {
  return <AnalyticsSkeleton />;
}
