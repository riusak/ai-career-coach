import { MockSkeleton } from '@/components/ui/dashboard-skeletons';

/** Chart 4 — instant loading state for the Mock Interviews segment. */
export default function Loading() {
  return <MockSkeleton />;
}
