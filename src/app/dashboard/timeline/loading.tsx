import { RoadmapSkeleton } from '@/components/ui/dashboard-skeletons';

/** Chart 4 — instant loading state for the Career Roadmap segment. */
export default function Loading() {
  return <RoadmapSkeleton />;
}
