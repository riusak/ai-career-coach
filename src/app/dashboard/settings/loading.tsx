import { SettingsSkeleton } from '@/components/ui/dashboard-skeletons';

/** Chart 4 — instant loading state for the Settings segment. */
export default function Loading() {
  return <SettingsSkeleton />;
}
