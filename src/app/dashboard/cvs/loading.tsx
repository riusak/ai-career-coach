import { CardGridPageSkeleton } from '@/components/ui/dashboard-skeletons';

/** Chart 4 — instant loading state for the CVs catalogue segment. */
export default function Loading() {
  return <CardGridPageSkeleton />;
}
