'use client';

/**
 * Moved to @/components/dashboard/MatchWithOfferModal during the Phase 2
 * dashboard migration (the modal is now shared with the dashboard quick
 * actions and the CV preview modal). This shim keeps the historical relative
 * import of the resume catalogue working.
 */
export { default, MatchOfferModal } from '@/components/dashboard/MatchWithOfferModal';
