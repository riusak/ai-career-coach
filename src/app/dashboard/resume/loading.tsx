import { ResumeCatalogueSkeleton } from '@/components/ui/dashboard-skeletons';

/** Chart 4 — instant loading state for the resume catalogue segment. */
export default function Loading() {
  return <ResumeCatalogueSkeleton />;
}
