import { ProfileFormSkeleton } from '@/components/ui/dashboard-skeletons';

/** Chart 4 — instant loading state for the Profile management segment. */
export default function Loading() {
  return <ProfileFormSkeleton />;
}
